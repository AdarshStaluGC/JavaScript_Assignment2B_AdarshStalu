<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Daily Workout Tracker</title>

  <!-- Link to Bootstrap CDN for simplicity -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  
  <!-- Link to custom CSS stylesheet -->
  <link rel="stylesheet" href="/stylesheets/style.css">

  <style>
    /* Black navbar */
    .navbar-black {
      background-color: #000 !important;
    }

    /* White button with white text */
    .btn-white {
      background-color: white;
      color: white;
      border: 1px solid white;
    }

    .btn-white:hover {
      background-color: black;
      color: white;
    }

    /* Optional full-page background setup for homepage */
    .hero-section {
      background-image: url('/images/your-image.jpg');
      background-size: cover;
      background-position: center;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      color: white;
    }

    .hero-section h1, .hero-section p {
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark navbar-black">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"> Daily Workout Tracker</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          {{#if user}}
            <li class="nav-item"><a class="nav-link" href="/workouts">Workout Plans</a></li>
            <li class="nav-item"><a class="nav-link" href="/auth/logout">Log Out</a></li>
          {{else}}
            <li class="nav-item"><a class="nav-link" href="/auth/login">Login</a></li>
            <li class="nav-item"><a class="nav-link" href="/auth/register">Register</a></li>
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>

  <!-- Flash messages -->
  <div class="container mt-4">
    {{#if success_msg}}<div class="alert alert-success">{{success_msg}}</div>{{/if}}
    {{#if error_msg}}<div class="alert alert-danger">{{error_msg}}</div>{{/if}}
    
    <!-- View-specific content -->
    {{{body}}}
  </div>
</body>
</html>
